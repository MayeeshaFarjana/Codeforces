#include<stdio.h>
int main()
{
    int n, h, a, cnt=0;
    scanf("%d %d", &n, &h);
    while(n--)
    {
        scanf("%d", &a);
        if(a <= h)
            cnt++;
        else
            cnt+=2;
    }
    printf("%d\n", cnt);
    return 0;
}

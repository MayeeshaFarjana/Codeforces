#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a,b;
    cin >> a >> b;
    if(a<b)
        cout << a << " "<<((b-a)/2) << "\n";
    else
        cout << b << " "<<((a-b)/2) << "\n";
    return 0;
}

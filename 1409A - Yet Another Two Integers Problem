#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int a, b, d;
        cin >> a >> b;
        d=abs(a-b);
        int ans=floor((d+9)/10);
        cout << ans << endl;
    }
    return 0;
}

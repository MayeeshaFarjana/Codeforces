#include<stdio.h>
int main()
{
    int i,n,cnt=0;
    char c[55];
    scanf("%d", &n);
    scanf("%s", &c);
    for(i=0; i<n-1; i++)
    {
        if(c[i]==c[i+1])
            cnt++;
    }
    printf("%d", cnt);
    return 0;
}

#include<stdio.h>
#include<string.h>
int main()
{
    int n,cnt=0;
    char x[5];
    scanf("%d", &n);
    while(n--)
    {
        scanf("%s", &x);
        if(x[1]=='-')
            cnt=cnt-1;
        else if(x[1]=='+')
            cnt=cnt+1;
    }
    printf("%d\n", cnt);
    return 0;
}

#include<stdio.h>
int main()
{
    int n, i, cnt=0;
    scanf("%d", &n);
    int a[n+5];
    for(i=0; i<n; i++)
    {
        scanf("%d", &a[i]);
        if(a[i]==1)
            cnt++;
    }
    if(cnt == 0)
        printf("EASY\n");
    else
        printf("HARD\n");
    return 0;
}

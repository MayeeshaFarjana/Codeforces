#include<stdio.h>
int main()
{
	long long int a, b, n, t;
	scanf("%lld", &t);
	while (t--)
	{
		scanf("%lld", &n);
		if (n % 2 == 0)
		{
			printf("%lld\n", (n / 2) - 1);
		}
		else if (n % 2 != 0)
		{
			printf("%lld\n", n / 2);
		}
	}
	return 0;
}

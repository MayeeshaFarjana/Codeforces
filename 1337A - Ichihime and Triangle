#include<stdio.h>
int main()
{
	int t;
	long long int a, b, c, d, x, y, z;
	scanf("%d", &t);
	while (t--)
	{
		scanf("%lld %lld %lld %lld", &a, &b, &c, &d);
		if (a <= b)
			x = b;
		if (b <= c)
			y = c;
		if (c <= d)
			z = c;
		printf("%lld %lld %lld\n", x, y, z);
	}
	return 0;
}

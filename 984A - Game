#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a[1005], n;
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    sort(a + 1, a + 1 + n);
    cout << a[(n >> 1) + (n & 1)] << endl;
    return 0;
}

#include<stdio.h>
int main()
{
    int t;
    long int n,s;
    scanf("%d", &t);
    while(t--)
    {
        scanf("%ld", &n);
        if(n%2==0)
            s=n/2;
        else if(n==1)
            s=1;
        else
            s=(n/2)+1;
        printf("%ld\n", s);
    }
    return 0;
}
